 const markup = `<li>
        <img src=${country.flags.svg} width=300px>
        <p> ${country.name.common}</p>
        <p>Capital: ${country.capital}</p>
        <p>Population: ${country.population}</p>
        <p>Languages: <ul> 
       <li>${country.languages.name}</li></ul>
        
      </p>
    </li>`;